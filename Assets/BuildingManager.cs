using UnityEngine;

// TODO: Building manager will place buildings and keep track of them


public class BuildingManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
